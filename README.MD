# StaffToggle

This plugin helps mitigate staff abuse, by adding/removing luckperms groups when the command is ran

## Usage:
Each player must have the `stafftoggle.use` permission
For every group to be toggled, they will also need the `stafftoggle.<group>` node
